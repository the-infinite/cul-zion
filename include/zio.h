#pragma once

/**
 * @brief Reads a file and returns a byte stream to that file.
 * 
 * @param filename The filename of the file we want to load.
 * @return A string which maps to the file we want to read.
 */
char* lmc_readFile(char* filename);
